spring:
  datasource:
    url: jdbc:h2:mem:b2bdb
    driver-class-name: org.h2.Driver
    username: ATT
    password: password

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true